<div style="background: url(/img/jumbotronbackground.jpg) no-repeat top; width: 100%; height: 50px;"></div>

<div class="container" style="margin-bottom: 40px;">
  <!-- Example row of columns -->
  <div class="row">
    <div class="col-md-12 tk-myriad-pro">
      <%= render partial: "shared/header", locals: {:title => 'Portfolio', :subtitle => 'The things I\'ve made'} %>
      <p>Given the nature of the industry that I currently work in, I am not able to publicly display the work I have
        completed. I do have a number of enterprise distributed iOS apps that I have programmed along with a universal
        iPhone/iPad app in the Apple App Store.</p>

      <p>One step removed from interpretive shadow puppets, what follows are the descriptions of some of my
        projects.</p>
      <hr>
    </div>
  </div>
  <div style="padding-bottom: 60px;">
  <% @platforms.order(:sortOrder).each do |platform| %>
      <div class="row">
        <div class="col-md-3">
          <p class="tk-adelle" style="font-size: 22px"><b><%= platform.title %></b></p>
        </div>
        <div class="col-md-9">
          <div class="row">
            <% platform.portfolio_entries.order(:sortOrder).each_with_index do |entry, i| %>
                <% if i % 3 == 0 && i > 0 %></div>
                    <div class="row">
                <% end %>
                <div class="col-md-4" style="margin: 0; padding: 0">
                  <div class="thumbnail">
                    <a style="-moz-border-radius: 10px; -webkit-border-radius: 10px; border-radius: 10px;" href="#project<%= i %><%= platform.title %>" name="thumb<%= i %><%= platform.title %>" class=""><%= image_tag entry.project_image.url(:thumb) %></a>
                    <div class="tk-adelle" style="margin: 0 auto; width: 310px; padding-left: 10px; font-weight:bold; color: #fff; margin-top: -30px;"><%= entry.title %></div>
                  </div>
                </div>
            <% end %>
            </div>
            </div>
      </div>
  <% end %>
  </div>
  <% @platforms.order(:sortOrder).each do |platform| %>
      <% platform.portfolio_entries.order(:sortOrder).each_with_index do |entry, i| %>
          <div style="background-color: <%= entry.backgroundColor %>; border-bottom: 4px solid #ffffff">
            <div class="row">
              <div class="col-md-12" name="project<%= i %><%= platform.title %>">
                <div style="position: relative; width:100%; overflow: hidden; background-position: top center; background-repeat: no-repeat; background-image: url(<%= entry.project_image.url(:original) %>>)">
                  <div style="position: relative; top: 20px; left: 40px; width: 60%; color: #fff; padding-bottom: 60px;">
                    <h3 class="tk-adelle" style="font-weight:bold;"><%= entry.title %></h3>
                    <h4 class="tk-myriad-pro"><%= entry.role %></span></h4>
                    <div class="tk-myriad-pro" style="margin: 25px 0 25px 0"><%= markdown(entry.overview) %></div>

                    <% if !entry.skill_tags.empty? %>
                        <div style="margin: 20px 0 25px 0">
                          <% entry.skill_tags.order("skill_tag_type_id ASC, tag ASC").each do |tag| %>
                              <span class="label <%= tag.skill_tag_type.tagType == ('Technology') ? 'label-success' : 'label-primary' %>"><%= tag.tag %></span>
                          <% end %>
                        </div>
                    <% end %>

                    <div><a class="btn btn-default" style="color: #ccc" href="#thumb<%= i %><%= platform.title %>"><span class="glyphicon glyphicon-chevron-up"></span></a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
      <% end %>
  <% end %>

</div> <!-- /container -->