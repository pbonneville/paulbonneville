<%= render partial: "shared/header_admin", locals: {:title => 'Editing User'} %>
<%= render 'form' %>
<%= render partial: "shared/admin_bottom_links", locals: {:links => [{:title => 'Show', :path => @user},
                                                                     {:title => 'Back', :path => users_path}] } %>
