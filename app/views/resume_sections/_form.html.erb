<%= form_for(@resume_section) do |f| %>
    <% if @resume_section.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@resume_section.errors.count, "error") %> prohibited this resume_section from being
            saved:</h2>

          <ul>
            <% @resume_section.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :title %>
      <br>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :sortOrder %>
      <br>
      <%= f.text_field :sortOrder %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
