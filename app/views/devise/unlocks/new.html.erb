<div class="container">
  <%= devise_error_messages! %>
  <%= render partial: "shared/header", locals: { :title => 'Resend Unlock Instructions' } %>

  <div class="row">
    <div class="col-md-3">
      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
          <div class="field">
            <%= f.label :email %>
            <%= f.email_field :email, autofocus: true %>
          </div>
          <div>
            <%= f.submit "Resend unlock instructions", :class => 'btn btn-success btn-lg' %>
          </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>